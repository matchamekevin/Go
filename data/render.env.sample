# Sample env file for Render deployment (DO NOT commit real secrets)
# Copy this file to render.env, fill values, and use with deploy script:

# Database connection (if using managed Postgres on Render, set DATABASE_URL instead)
DB_HOST=your-db-host.render.com
DB_PORT=5432
DB_USER=gosotral_user
DB_PASSWORD=gosotral_pass
DB_NAME=gosotral_db

# Alternatively, use a single DATABASE_URL
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# JWT and security
JWT_SECRET=replace_with_secure_random

# SMTP (if used)
# Email configuration (for development)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=2003gnati24@gmail.com
SMTP_PASS=aepl clfx zpvw zndc 
SMTP_FROM="GoSOTRAL <2003gnati24@gmail.com>"
# SMTP_USER=your_smtp_user

# App settings
NODE_ENV=production
PORT=7000
